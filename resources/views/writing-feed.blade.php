@php echo '<'.'?xml version="1.0" encoding="UTF-8"?'.'>'; @endphp
<rss version="2.0"><channel><title>Birdcar — Writing</title><link>{{ route('public.writing') }}</link><description>Ideas about making a business work better.</description><language>en</language>@foreach ($articles as $article)<item><title>{{ $article['title'] }}</title><link>{{ route('public.article', ['slug' => $article['slug']]) }}/</link><guid>{{ route('public.article', ['slug' => $article['slug']]) }}/</guid><description>{{ $article['description'] }}</description><pubDate>{{ $article['date']->toRfc2822String() }}</pubDate></item>@endforeach</channel></rss>
