---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="tag-list">
  {tags.map((tag) => (
    <li class="tag-list__item"><a href={`/tags/${tag}/`}>#{tag}</a></li>
  ))}
</ul>
