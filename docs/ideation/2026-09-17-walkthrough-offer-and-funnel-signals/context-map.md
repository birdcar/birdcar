# Context Map: 2026-09-17-walkthrough-offer-and-funnel-signals

**Phase**: 3
**Gates**: 5/5 ready
**Verdict**: GO

## Gates

### Phase 3 (current)

| Gate                 | Status | Evidence                                                |
| -------------------- | ------ | ------------------------------------------------------- |
| Scope clarity        | ready  | Every file in the spec's Modified Files table was read and each edit pinned to a line: `resources/views/pages/index.blade.php:15,17,33,42,46-48,53`; `resources/views/pages/walkthrough.blade.php:13-14,16,22-24,31,37-38,44-49 (+ one new <details> after :49),54`; `resources/views/components/marketing/assessment-invitation.blade.php:5,7`; `tests/Feature/MarketingSiteTest.php:14-26,37-48` (anchors) plus two new tests and one helper; `PRODUCT.md:334-344` (append bullets) and `:346-350` (prune first two Open Decisions); `.ai/rules/resources.md:12` (replace the `Assessment booking uses` sentence). `rg` over `tests/` for every current string the copy rewrites (`A report to keep`, `Something to work from`, `A practical report`, `written report you keep`, `I work with you`, `That’s where I want to start`) found nothing, so no existing assertion breaks. Phase 2 is committed (`a4c5099`): `walkthrough.blade.php` exists, `placement="walkthrough-hero"`, config/tests on `birdcar/walkthrough`. |
| Pattern familiarity  | ready  | Read the anchor-assertion pattern at `MarketingSiteTest.php:14-26,37-48` (chained `assertSee`, curly `’`), the dataset/lowercase-`expect` idiom at `:50-63`, the only top-level helper precedent `tests/Feature/Authorization/OrganizationMembershipTest.php:183-194` (after the tests, PHPDoc, return type), the FAQ row shape `<details><summary>Q<x-marketing.arrow /></summary><p>A</p></details>` at `walkthrough.blade.php:44-49`, the `<dl>` rows at `:22-24`, `<main id="main">` at `layout.blade.php:36`, `MarketingSite::head()` at `MarketingSite.php:72-86` (laravel/head 0.2.2 emits title/meta/JSON-LD `<script>`), PRODUCT.md bullet style `:334-344`, resources.md frontmatter `:1-4`, and the testing skill's `naming.md`, `assertions.md`, `review.md`. |
| Dependency awareness | ready  | `rg` mapped every consumer: the invitation renders at `index.blade.php:55` and `work.blade.php:30` (own heading); body copy is asserted only by `MarketingSiteTest.php:14-26,28-35,37-48,50-57,59-63,149-159`; `MarketingAnalyticsTest.php:27-31,36` asserts placement markers on the booking-links (markup the spec keeps); `MarketingDiscoveryTest.php:19,49` asserts og:url and the Service schema, both head-driven, not body prose; the contract's acceptance filters (`contract.md:26-27`) bind the two new test names; `.ai/rules/index.md:9` maps `resources/**` → resources.md; PRODUCT.md is read only by agents (`resources.md:9`), never by code. |
| Edge case coverage   | ready  | Concrete Phase 3 list below, grounded in a scratchpad simulation of the spec's exact copy against the spec's own regexes (layout included): `/walkthrough` lands reader 39 vs first-person 39 (a tie, not the spec's 41:37; the spec's step-two flip gives 40:37), `/` lands 21:19 under bare `strip_tags` and 24:19 when tags become spaces (three `You` at `</h3><p>` junctions lose their `\b`); homepage `<main>` has 0 `?` today and exactly 2 after the copy; the failure message only prints counts while failing; residual stale lines at `PRODUCT.md:79,84-85,101-102` and `resources.md:15`; `record-rule` availability; curly-apostrophe anchors; helper-name global uniqueness. |
| Test strategy        | ready  | Pest v5.1.4 on PHP 8.5.8; `phpunit.xml:6` `failOnEmptyTestSuite="true"`; neither `speaks to the reader`, `asks the reader`, nor `marketingProse` exists in `tests/` today, so the filtered acceptance runs fail until the tests land. Commands: `php artisan test --compact tests/Feature/MarketingSiteTest.php` (inner loop); `php artisan test --compact tests/Feature/MarketingSiteTest.php --filter='speaks to the reader'`; `--filter='asks the reader'`; `php artisan test --compact tests/Feature/MarketingSiteTest.php tests/Feature/MarketingDiscoveryTest.php tests/Feature/MarketingAnalyticsTest.php`; `vendor/bin/pint --dirty --format agent`; the spec's scarcity grep (exit 1 = clean today and on the simulated copy); `grep -n 'three business days' PRODUCT.md .ai/rules/resources.md` (0 hits today, must be ≥2 after); `rg -n -w -i 'we' resources/views/pages resources/views/components/marketing` (0 hits today) as the mechanical form of the "no corporate we" manual item. |

### Phase 2 (prior, for reference)

| Gate                 | Status | Evidence                                                |
| -------------------- | ------ | ------------------------------------------------------- |
| Scope clarity        | ready  | Every file in the spec's Modified Files table was read and each edit pinned to a line: `config/marketing.php:6-8`; `app/Services/MarketingSite.php:34,58,64`; `resources/views/pages/assessment.blade.php:5,8,14,15,20,44,49` (to be `git mv`'d to `walkthrough.blade.php`); `components/marketing/layout.blade.php:1,7,24,32,42`; `booking-link.blade.php:6`; `assessment-invitation.blade.php:6`; `pages/index.blade.php:16,17,33,34`; `routes/web.php:66,71`; `tests/Feature/MarketingSiteTest.php:20,36-56,138`; `MarketingDiscoveryTest.php:17-19,42-49,70,103`; `MarketingAnalyticsTest.php:34-36`. A repo-wide `rg -in assessment` (excluding vendor/node_modules/docs) found no prose instance outside this list; survivors are CSS classes/ids, the `assessment-hero` placement marker, the component filename, and PRODUCT.md/DESIGN.md prose that Phase 3 or the out-of-scope list owns. |
| Pattern familiarity  | ready  | Read `routes/web.php:64-67` (three `Route::permanentRedirect` calls inside the `Route::domain(marketing host)->name('public.')` group), `resources/views/pages/work.blade.php:1-8` (`use function Laravel\Folio\name; name('public.work');` then `<x-marketing.layout title=... active=... description=...>`), the existing dataset redirect test at `MarketingSiteTest.php:133-139`, and Folio v1.2.0's name resolution in `vendor/laravel/folio/src/FolioRoutes.php:59-107`. |
| Dependency awareness | ready  | `rg` mapped every consumer of what changes: `public.assessment` is referenced at `layout.blade.php:42`, `booking-link.blade.php:6`, `routes/web.php:71`, `MarketingSiteTest.php:20,51`; `$active === 'assessment'` at `layout.blade.php:7,24,32` (set only by `assessment.blade.php:8`); `config('marketing.booking_*')` at `booking-link.blade.php:6` and `assessment.blade.php:55-56` (JS reads namespace from DOM attributes, no `assessment` string in `resources/js`); `MarketingSite::head()` has one caller (`layout.blade.php:7`, positional args, so the `$assessment` → `$walkthrough` rename has no caller impact); `/assessment` path is consumed by `routes/web.php:66` and the three test files. Folio's page handler is `Route::fallback` (`FolioManager.php:74`), so `web.php` redirects always win. |
| Edge case coverage   | ready  | Concrete Phase 2 list below: `route:list` never shows Folio pages (spec manual check is unpassable as written), in-process Folio name map on the long-running dev server, `Route::fallback` ordering, dataset-vs-new redirect test and the `--filter='walkthrough'` match, the `assessment-hero` placement marker the spec neither renames nor lists as kept, `serviceType` retained, the negative test not catching `free business assessment` or the old meta description (spec changes both anyway), stale `.ai/rules/resources.md` steering against the rename, Cal.com slug not existing yet. |
| Test strategy        | ready  | Pest v5 feature tests; `phpunit.xml:6` has `failOnEmptyTestSuite="true"` and `:36-38` pin `POSTHOG_DISABLED=true`, blank token, host (Phase 1 landed in commit `d75312a`). Commands: `php artisan test --compact tests/Feature/MarketingSiteTest.php tests/Feature/MarketingDiscoveryTest.php tests/Feature/MarketingAnalyticsTest.php`; `php artisan test --compact tests/Feature/MarketingSiteTest.php --filter='walkthrough'`; `php artisan folio:list` (confirmed present, currently shows `/assessment → public.assessment`); `php artisan route:list --name=public` (currently 5 routes, will be 6); the two `grep` validation lines from the spec; `vendor/bin/pint --dirty --format agent` after the `MarketingSite.php` edit. Testing skill lives at `/Users/birdcar/Code/birdcar/birdcar/.claude/skills/testing-best-practices`. |

### Phase 1 (prior, for reference)

| Gate                 | Status | Evidence                                                |
| -------------------- | ------ | ------------------------------------------------------- |
| Scope clarity        | ready  | Every file in the spec's File Changes table was read and the exact insertion points confirmed: `app/Services/PostHogService.php` (append after `withContext()` at :79-86), `resources/views/components/marketing/layout.blade.php` (:15 body tag, :23 and :31 nav booking links), `booking-link.blade.php` (:1-6, whole file), `pages/index.blade.php` (:16, :34), `pages/assessment.blade.php` (:15 raw hero anchor, :56 fallback anchor), `components/marketing/assessment-invitation.blade.php` (:6), `resources/js/app.js` (:2-4), `resources/js/booking.js` (after the `'ui'` call at :86-93), `phpunit.xml` (:2-6 root attributes, :20-35 `<php>` block), `package.json` + `bun.lock` (`posthog-js` absent today). |
| Pattern familiarity  | ready  | Read the constructor enabled-state logic in `PostHogService.php:14-41`, the `@php(...)`/`@class` idiom in `layout.blade.php:7,15`, the `config([...])` then `$this->get()` Pest pattern in `tests/Feature/MarketingDiscoveryTest.php:14-21`, the namespaced `Cal.ns[namespace](...)` queue API in `booking.js:8-45,86-103`, and the ESM named-export style in `app.js:1-4`. |
| Dependency awareness | ready  | `rg` mapped every consumer: `PostHogService` has 8 call sites (all additive-safe), `booking-link` has 5 call sites (all rewritten by this phase), `booking.js` has 1 importer (`app.js`), the layout wraps 5 Folio pages; `MarketingSiteTest.php:36-56` holds the markup assertions that the Blade edits must keep green. |
| Edge case coverage   | ready  | Concrete list below covers the debug-mode throw with `.env` `APP_DEBUG=true`, non-singleton service + static `$initialized`, capture loss on navigating clicks, `forwardQueryParams` leaking opt-out params into the Cal iframe, the exact-string assertion at `MarketingSiteTest.php:39`, and the assessment-page inline/modal mode split. |
| Test strategy        | ready  | Pest v5.1.4 on PHPUnit 13.3.2; `vendor/phpunit/phpunit/phpunit.xsd:233` declares `failOnEmptyTestSuite`; `tests/Pest.php:17-19` binds Feature tests to `Tests\TestCase` + `RefreshDatabase` on in-memory sqlite (`phpunit.xml:26-27`); commands are `php artisan test --compact tests/Feature/MarketingAnalyticsTest.php tests/Feature/MarketingSiteTest.php tests/Feature/MarketingDiscoveryTest.php`, `bun run build` + the five `grep -lE ... public/build/assets/app-*.js` checks, and `php artisan test --compact --filter='no such test name'; test $? -ne 0`. |

## Key Patterns

### Phase 3

- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/MarketingSiteTest.php:14-26,37-48` — flat `test('lowercase sentence', function () { $this->get($path)->assertOk()->assertSee(...) })` chains. Prose anchors use plain `assertSee` (value passes through `e()`, which leaves `’` untouched); raw HTML uses `assertSee(..., false)`. New homepage anchors slot into `:14-26` (which already asserts `I help businesses untangle work`, `fifteen years`, `Book a free Walkthrough`, and the GHX/DataDash/`more than fifteen years` negatives); new walkthrough anchors slot into `:37-48` (`:45-46` are the two "existing, keep" anchors). `:59-63` is the `expect(mb_strtolower(...))->not->toContain(...)` idiom over a dataset — the closest sibling to the ratio test's `expect($reader)->toBeGreaterThanOrEqual($me, $message)` with `->with(['/', '/walkthrough'])`.
- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/Authorization/OrganizationMembershipTest.php:183-194` — the project's only precedent for a top-level helper in a Pest test file: declared after the last test, PHPDoc block, explicit return type, camelCase name. `tests/Pest.php:47-50` holds only the empty `something()` stub, so helpers live in the test file. Pest loads every test file into one PHP process, so `marketingProse` must be globally unique (`rg '^function ' tests/` → three other helpers, no collision).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/index.blade.php` — target lines: `:15` `.hero-explanation`; `:17` `.assessment-terms`; `:30` strip heading (stays); `:32` strip p1 (stays verbatim in the spec); `:33` strip p2 → becomes two `<p>` (the two questions, then the turnaround paragraph); `:41` approach p1 (stays); `:42` approach p2; `:46-48` three single-line `<section id="…"><h3>…</h3><p>…</p></section>` steps; `:53` personal note — two `<p>` inside one `<div>` on one line, the second `<p>` stays. Ids `home-heading`, `how-i-work`, `understand`, `build`, `care`, `personal-heading` and every class stay (`MarketingSiteTest.php:155,158` XPath `#how-i-work`).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/walkthrough.blade.php` — target lines: `:13-14` opening `<p>`s, plus one new `<p>` (no-pitch line) between `:14` and the CTA at `:15`; `:16` `.action-note`; `:22-24` `<div><dt>…</dt><dd>…</dd></div>` rows (`:23` stays, `:24` `Something to work from` → `Where I’d start`); `:31` outcome `<div><p>…</p><p>…</p></div>` on one line; `:37-38` steps 2-3 as `<li><span class="step-number">N</span><div><h3>…</h3><p>…</p></div></li>` (`:36` step 1 stays); `:44-49` FAQ rows `<details><summary>Q<x-marketing.arrow /></summary><p>A</p></details>` — the new "replacing my team" row goes after `:49` with the same arrow; `:54` closing `<p>…<br>Yours to use however you choose.</p>`. `:55-56` (calendar div and fallback link) are asserted verbatim at `MarketingSiteTest.php:40-41` — do not touch.
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/assessment-invitation.blade.php:1-9` — `@props(['heading' => 'Show me the part that keeps getting stuck.'])`, `:5` body `<p>`, `:6` booking-link (stays, `placement="closing-invitation"`), `:7` `.action-note`. Rendered by `index.blade.php:55` (default heading) and `work.blade.php:30` (`heading="What’s getting in the way of your work?"` — that `?` renders on `/work`, not `/`).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/layout.blade.php:21,29,36` — `How I work` appears twice (desktop and mobile nav) and is the only first-person text outside the page slot (layout alone counts reader 0 / first-person 2); `<main id="main" tabindex="-1">{{ $slot }}</main>` is the single `<main>` the question test regexes; skip link, wordmark, footer (`Thoughtful systems. Room for better work.`, `The Walkthrough`) carry no pronouns or `?`.
- `/Users/birdcar/Code/birdcar/birdcar/app/Services/MarketingSite.php:72-86` — `laravel/head` 0.2.2 emits `<title>`, description/og/twitter `<meta>` (attributes, removed by `strip_tags`) and each `Head::schema()` entity as `<script type="application/ld+json">` (removed by the helper's `<(script|style)…>` regex). The JSON-LD `description` on `/` is the layout default `I help businesses untangle work … a written report you keep` (`layout.blade.php:1`), reachable only through that script — the spec's script strip is what keeps it out of the ratio.
- `/Users/birdcar/Code/birdcar/birdcar/PRODUCT.md:334-344,346-358` — "Approved Launch Copy and Proof" is a flat `-` bullet list with typographic quotes (`“I help businesses”`); "Open Decisions" bullets, the two to prune at `:348-350`. `:309-310` already frames GitHub/Heroku/Zapier/WorkOS as "background associations; they do not imply those companies endorse this offer" — the wording the spec's "Some of that work was for" leans on; `:139` records the WorkOS employment; `:192-193` (Product Principles) is the source of the replacing-people answer.
- `/Users/birdcar/Code/birdcar/birdcar/.ai/rules/resources.md:1-4,12,15` — YAML frontmatter `paths: ['resources/**']`; `:12` is one paragraph whose last sentence begins `Assessment booking uses` (replace only that sentence); `:15` also says "Preserve approved copy, free assessment/report offer" (see Risks). `.ai/rules/index.md:9` maps the glob to this file; `CLAUDE.md:94` prefers Boost's `record-rule` (glob, title, note) for writes.
- `/Users/birdcar/Code/birdcar/birdcar/.claude/skills/testing-best-practices/rules/naming.md:12-17,37-45` — keep the file's `test()` style, no `describe()` for one flow (the existing marketing tests have none); `assertions.md:29` says not to stack `assertOk()` before `assertSee`, but the file's convention does and `SKILL.md:18` says convention wins; `review.md:41` — expected values must be known values (the ratio's threshold is the other measured operand, which the contract explicitly wants).

### Phase 2

- `/Users/birdcar/Code/birdcar/birdcar/routes/web.php:64-85` — the marketing group is `Route::domain(app(MarketingSite::class)->host())->name('public.')->group(...)`; redirects are one-liners `Route::permanentRedirect('/case-studies', '/work');` (:65-67) placed before the `/sitemap.xml` closure; the sitemap iterates a literal array of route names at :71 (`['public.index', 'public.work', 'public.assessment', 'public.writing']`) and resolves each with `route($route, absolute: false)` — Folio names resolve here through `UrlGenerator::resolveMissingNamedRoutesUsing` (`vendor/laravel/folio/src/FolioServiceProvider.php:71`).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/work.blade.php:1-8` — Folio page header convention: `<?php use function Laravel\Folio\name; name('public.work'); ?>` then a single `<x-marketing.layout title="..." active="work" description="...">` wrapper. `assessment.blade.php:1-8` follows it exactly; only the name string, `active` value, `title`, and `description` change.
- `/Users/birdcar/Code/birdcar/birdcar/vendor/laravel/folio/src/FolioManager.php:74` — Folio registers `Route::fallback([self::class, 'handle'])->name('laravel-folio')`. Laravel matches fallback routes only after every explicit route fails, so `Route::permanentRedirect('/assessment', '/walkthrough')` in `web.php` beats a page file at `/assessment` regardless of registration order. This is stronger than the spec's "the `/contact` redirect proves web.php wins" argument.
- `/Users/birdcar/Code/birdcar/birdcar/vendor/laravel/folio/src/FolioRoutes.php:59-107` — `route('public.x')` for a Folio page scans every `*.blade.php` under the mount path with `Finder`, reads each file's `name()` call, and stores the map in the `FolioRoutes` instance (`$this->loaded`). It only reads `bootstrap/cache/folio-routes.php` if that file exists and matches `static::$version`; `ls bootstrap/cache/` shows only `.gitignore`, `packages.php`, `services.php`, so there is no cache to clear locally. The cache is written only by `route:cache` (`FolioServiceProvider.php:92-95`).
- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/MarketingSiteTest.php:133-139` — redirect assertions already exist as a dataset test: `test('the old marketing entry points redirect to the new destinations', function (string $from, string $to) { $this->get($from)->assertStatus(301)->assertRedirect($to); })->with([...])`. Adding `['/assessment', '/walkthrough']` and retargeting `['/contact', '/walkthrough']` follows the pattern; a separate test is not required.
- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/MarketingDiscoveryTest.php:42-52` — the Service schema test parses `//script[@type="application/ld+json"]` with DOMXPath, keys by `@type`, and asserts `toMatchArray(['name' => 'Free business assessment', 'serviceType' => 'Business process assessment'])`; only `name` changes.
- `/Users/birdcar/Code/birdcar/birdcar/app/Services/MarketingSite.php:34-70` — `head(string $title, string $description, string $path, ?CarbonInterface $publishedAt = null, bool $assessment = false)`; the `if ($assessment)` block at :58-70 builds the `Service` entity with `'name' => 'Free business assessment'` (:64) and `'serviceType' => 'Business process assessment'` (:65). Rename the parameter and the `if`, change `name`, keep `serviceType`.

### Phase 1

- `/Users/birdcar/Code/birdcar/birdcar/app/Services/PostHogService.php:14-41` — constructor is the single owner of enabled state: `config('posthog.disabled')` short-circuits first, then `blank($apiKey)` / `blank($host)` each call `throwWhenDebugging()` (:88-93, throws `RuntimeException` only when `config('app.debug')` is true), then a static `self::$initialized` guard around `PostHog::init`. Every public method (:43-86) starts with `if (! $this->enabled) { return ...; }`. Explicit return types on every method; no PHPDoc on existing methods, so the spec's `@return array{token: string, host: string}|null` will be the first array-shape doc in the file (matches the Boost PHP rule).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/layout.blade.php:7,15,21-23` — `@php(app(\App\Services\MarketingSite::class)->head(...))` resolves a service inline with FQCN; `<body @class([...])>` is the attribute-directive idiom; inline `@if ($active === 'work') aria-current="page" @endif` inside a tag (:21) is the existing precedent for the spec's conditional `data-posthog-*` attributes on `<body>`.
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/booking-link.blade.php:1-6` — `@props(['inline' => false])` on line 1, then two `<a {{ $attributes->merge([...]) }}>` branches: inline → `href="#choose-a-time"`, modal → `href="{{ route('public.assessment') }}"` plus `data-cal-link`, `data-cal-namespace`, `data-cal-config` (JSON). The spec's `...$marker` spread into `merge()` fits; `placement` declared in `@props` is consumed and will not leak as an HTML attribute.
- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/MarketingDiscoveryTest.php:14-21,66-73` — Pest `test('sentence...', function () { config([...]); $this->get('/path')->assertSee('<raw html>', false); })`. Datasets via `->with([...])`. `CarbonImmutable::setTestNow` in `beforeEach`/`afterEach` only where dates matter (not needed for the analytics test). No `describe()` blocks anywhere in the existing marketing tests — flat `test()` calls; the spec's "describe block" suggestion is optional and would be a first in this suite.
- `/Users/birdcar/Code/birdcar/birdcar/resources/js/booking.js:8-45,67-104` — `ensureCal()` installs Cal.com's queueing loader; `Cal.ns[namespace]` is a function whose calls push onto `api.q` until `embed.js` drains them, so `Cal.ns[namespace]('on', {...})` queued right after the `'ui'` call (:86-93) is safe before the script loads. `initBooking()` computes `inline`, `trigger`, `source`, `namespace` (:68-74) — `mode = inline ? 'inline' : 'modal'` slots in after :93. `keepTriggersOnPage()` (:52-61) calls `event.preventDefault()` on `a[data-cal-link]` once `cal-modal-box` is registered; it does not stop propagation, so a document-level analytics listener still fires.
- `/Users/birdcar/Code/birdcar/birdcar/resources/js/app.js:1-4` — `import { initBooking } from './booking'; initBooking();` — relative ESM import without extension, named exports, top-level invocation. `initAnalytics()` goes between :2 and :4 so its click listener registers before booking's.
- `/Users/birdcar/Code/birdcar/birdcar/.agents/skills/integration-laravel/references/laravel.md` and `identify-users.md` — PHP SDK shapes only; the one browser note (`laravel.md:67`) concerns `tracing_headers`, which this project does not use. Nothing here about posthog-js init options; rely on the spec's option list.

## Dependencies

### Phase 3

- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/index.blade.php` (prose at :15,17,33,42,46-48,53) — consumed by → `tests/Feature/MarketingSiteTest.php:14-26` (anchors `I help businesses untangle work`, `fifteen years`, `Book a free Walkthrough`, `route('public.walkthrough')`; negatives GHX, DataDash, `more than fifteen years`, `Your AI wrote a bug`), `:50-57` (booking-link attributes on `/`), `:59-63` (lowercased content must not contain `free assessment`), `:149-159` (XPath `#how-i-work`, header wordmark, `href="…#how-i-work"`), `MarketingAnalyticsTest.php:27-31` (`data-booking-cta` `header`, `mobile-menu`, `hero`, `homepage-strip`, `closing-invitation` — all on booking-link components the spec keeps), `MarketingDiscoveryTest.php` (canonical/og/JSON-LD/sitemap — head-driven). No Blade file includes the page by name; Folio serves it.
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/walkthrough.blade.php` (prose at :13-14,16,22-24,31,37-38,44-49,54) — consumed by → `MarketingSiteTest.php:37-48` (raw Cal attributes at :40-41 verbatim; `id="choose-a-time"`, `href="#choose-a-time"`; prose anchors :45-46 the spec keeps; `assertDontSee('You’re booked')`), `:59-63`, `MarketingAnalyticsTest.php:36` (`walkthrough-hero`), `MarketingDiscoveryTest.php:19` (og:url) and `:49` (Service `name`/`serviceType` come from `MarketingSite.php:64-65`, not the page body). `active="walkthrough"` at `:8` drives `layout.blade.php:7,24,32` — untouched by this phase.
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/assessment-invitation.blade.php:5,7` — consumed by → `index.blade.php:55`, `work.blade.php:30`; asserted indirectly by `MarketingSiteTest.php:28-35` (`/work` must not say `retainer`, GHX, DataDash), `:50-57` (`/work` booking-link attributes), `:59-63` (`/work` no `free assessment`), `MarketingAnalyticsTest.php:31`. Filename stays (contract).
- `/Users/birdcar/Code/birdcar/birdcar/tests/Feature/MarketingSiteTest.php` (two new tests + `marketingProse()`) — consumed by → `contract.md:26-27` acceptance commands `--filter='speaks to the reader'` and `--filter='asks the reader'` (names are load-bearing); `phpunit.xml:6` `failOnEmptyTestSuite` turns a misnamed test into a failure; `vendor/bin/pint --dirty` formats it.
- `/Users/birdcar/Code/birdcar/birdcar/PRODUCT.md:334-350` — consumed by → agents only (`.ai/rules/resources.md:9` "Use PRODUCT.md for product truth and written voice"); `rg PRODUCT.md app resources tests` finds no code reader.
- `/Users/birdcar/Code/birdcar/birdcar/.ai/rules/resources.md:12` — consumed by → `.ai/rules/index.md:9` (glob `resources/**` → this file; unchanged unless `record-rule` creates a new file), every agent editing `resources/**`, and the Boost `record-rule` tool.
- Phase 4 (human gate) depends on this phase's PRODUCT.md bullet naming `https://cal.com/birdcar/walkthrough` and namespace `walkthrough` (the event the owner creates) and on the fourteen anchors being the exact strings the owner reads against the seven sweeps.

### Phase 2

- `/Users/birdcar/Code/birdcar/birdcar/config/marketing.php:6-8` (`booking_url`, `booking_calendar`, `booking_namespace`) — consumed by → `resources/views/components/marketing/booking-link.blade.php:6` (`data-cal-link`, `data-cal-namespace` on the modal branch), `resources/views/pages/assessment.blade.php:55` (inline `<div class="booking-calendar">`) and `:56` (fallback `href`). `resources/js/booking.js` reads the namespace from `data-cal-namespace` at runtime; `rg -in assessment resources/js` returned nothing, so no JS edit is needed. Tests asserting the values: `MarketingSiteTest.php:39,40,52,53`.
- `/Users/birdcar/Code/birdcar/birdcar/app/Services/MarketingSite.php:34` (`head()` fifth parameter) and `:64` (Service `name`) — consumed by → `resources/views/components/marketing/layout.blade.php:7` (only caller; passes `$active === 'assessment'` positionally, so renaming the parameter changes nothing at the call site beyond the `'walkthrough'` comparison), `tests/Feature/MarketingDiscoveryTest.php:49` (asserts `name` and `serviceType`).
- Route name `public.assessment` (declared at `resources/views/pages/assessment.blade.php:5`) — consumed by → `layout.blade.php:42` (footer link), `booking-link.blade.php:6` (modal fallback `href`), `routes/web.php:71` (sitemap array), `tests/Feature/MarketingSiteTest.php:20,51`. Resolution path: `FolioServiceProvider.php:71` → `FolioRoutes::get()` → in-process scan of `resources/views/pages` (`FolioRoutes.php:89-104`). A missed reference throws `RouteNotFoundException` and 500s every page that renders the layout, which the first test run catches.
- `$active` value `'assessment'` (set at `assessment.blade.php:8`) — consumed by → `layout.blade.php:7` (Service schema toggle), `:24` (`:inline` and `:aria-current` on the desktop nav link), `:32` (`:inline` on the mobile-menu link). All three comparisons and the one producer must change together or the `/walkthrough` page loses its inline nav branch and Service schema.
- Path `/assessment` — consumed by → `routes/web.php:66` (`/contact` redirect target), `MarketingSiteTest.php:37,138`, `MarketingDiscoveryTest.php:17-19,43,70,103`, `MarketingAnalyticsTest.php:35`. After the move `/assessment` is served only by the new `permanentRedirect`.
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/pages/assessment.blade.php` → `walkthrough.blade.php` — Folio page (currently `folio:list` row `GET localhost/assessment .. public.assessment › assessment.blade.php`); no Blade `@include`/`@extends` references the file by name (only Folio's directory scan). `data-booking-cta="assessment-hero"` at :15 is consumed by `MarketingAnalyticsTest.php:36` and by the Phase 1 analytics listener generically (it reads whatever value the attribute carries).
- `/Users/birdcar/Code/birdcar/birdcar/resources/views/components/marketing/assessment-invitation.blade.php` — consumed by → `pages/index.blade.php:55`, `pages/work.blade.php:30`. Filename stays (contract: "the component keeps its filename"); only the button text at :6 changes in this phase. Phase 3 edits :5 and :7.
- Phase 3 (`spec-phase-3.md:8,46,76,95-106,168`) depends on this phase's outputs by name: `resources/views/pages/walkthrough.blade.php`, the `/walkthrough` path in its datasets, and the button string `Book a free Walkthrough` ("stays, Phase 2"). Phase 3 also owns `PRODUCT.md`, `.ai/rules/resources.md`, and the `assessment-invitation` action note.

### Phase 1

- `app/Services/PostHogService.php` (new `isEnabled()`, `browserConfig()`) — consumed by → `app/Http/Middleware/PostHogRequestContext.php:13` (constructor injection, runs on every `web`/`api` request via `bootstrap/app.php:31-32`), `bootstrap/app.php:40` (`captureException` in the exception reporter), `app/Providers/FortifyServiceProvider.php:49`, `app/Actions/Fortify/CreateNewUser.php:44`, `ResetUserPassword.php:33`, `UpdateUserPassword.php:36`, `UpdateUserProfileInformation.php:46`. All call existing methods; the two additions are purely additive. `PostHogService` is not bound as a singleton or scoped anywhere in `app/Providers/` (rg found no `singleton|scoped|bind(`), so `app(PostHogService::class)` constructs a fresh instance and re-reads config on every resolution.
- `resources/views/components/marketing/booking-link.blade.php` (new optional `placement` prop) — consumed by → `layout.blade.php:23` (desktop nav, `class="nav-booking"`), `layout.blade.php:31` (mobile menu), `pages/index.blade.php:16` (hero), `pages/index.blade.php:34` (homepage strip), `components/marketing/assessment-invitation.blade.php:6` (closing invitation). All five are rewritten by this phase; default `null` keeps any caller without `placement` rendering as today.
- `resources/views/components/marketing/layout.blade.php` (body attributes) — consumed by → `pages/index.blade.php`, `pages/assessment.blade.php`, `pages/work.blade.php`, `pages/writing/index.blade.php`, `pages/writing/[slug].blade.php`. Folio mounts these on the marketing host only (`app/Providers/FolioServiceProvider.php:16-17`).
- `resources/js/booking.js` (`initBooking` unchanged signature; new import of `./analytics`) — consumed by → `resources/js/app.js:2,4` only.
- `resources/js/app.js` — Vite entry via `vite.config.js:9` (`laravel({ input: ['resources/css/app.css', 'resources/js/app.js'] })` under `vite-plus`); emitted as `public/build/assets/app-*.js`, which is gitignored (`.gitignore:3`).
- `phpunit.xml` — consumed by every Pest run; `<php>` block currently pins 14 env vars (:21-34) and nothing for `POSTHOG_*` or `APP_DEBUG`.
- Tests asserting on markup this phase touches: `tests/Feature/MarketingSiteTest.php:36-47` (`/assessment`: `href="#choose-a-time"`, exact string `data-cal-inline data-cal-link="birdcar/free-assessment" data-cal-namespace="free-assessment"`, `href="https://cal.com/birdcar/free-assessment"`, `assertDontSee('data-cal-config')`), `:49-56` (`/`, `/work`, `/writing/` carry `data-cal-link`/`data-cal-namespace`/`data-cal-config=` and not `data-cal-inline`), `:141-151` (XPath on header/footer). No existing test references `posthog` or `data-booking` (rg over `tests/` and `resources/` returned nothing).

## Conventions

- **Naming**: Blade components kebab-case under `resources/views/components/marketing/` and referenced as `<x-marketing.name>`; Folio pages are `resources/views/pages/{slug}.blade.php` with route names `public.{slug}` declared via `name()` on line 5; JS modules lowercase single-word files in `resources/js/`, named exports (`initBooking`), module constants in SCREAMING_CASE (`EMBED_SCRIPT`); PHP methods camelCase with descriptive verbs (`captureException`, `withContext`); Pest test names are full sentences in lowercase.
- **Imports**: JS uses relative ESM without extensions (`'./booking'`); `package.json` is `"type": "module"` with runtime libs such as `motion` in `dependencies`; Blade resolves services with FQCN inside `@php(app(\App\Services\X::class))`; PHP uses `use` imports and `config('dot.path')`; `routes/web.php` imports `MarketingSite` and resolves the host with `app(MarketingSite::class)->host()` at route-definition time.
- **Error handling**: service methods guard with early `return` when disabled; the constructor throws `RuntimeException` only under `app.debug`; no try/catch in the marketing layer; JS returns early (`if (!source) return;`) rather than throwing.
- **Types**: PHP explicit parameter and return types on every method; PHPDoc array shapes per the Boost PHP rules; JS is untyped ESM with no linter or type checker configured (no `lint`/`check` script in `package.json`).
- **Testing**: Pest v5 feature tests in `tests/Feature/*.php`, bound to `Tests\TestCase` + `RefreshDatabase` (`tests/Pest.php:17-19`), sqlite `:memory:`; raw-HTML assertions use `assertSee('...', false)`; prose assertions use plain `assertSee` with the template's curly apostrophes (`’`, which `e()` leaves untouched — see `MarketingSiteTest.php:46`); config overrides via `config([...])` before the request; datasets via `->with([...])`; redirect checks are `assertStatus(301)->assertRedirect($to)`; no `describe()` blocks in existing marketing tests; per project rules read the `testing-best-practices` skill (`.claude/skills/testing-best-practices`) before writing tests and run `vendor/bin/pint --dirty --format agent` after PHP edits.
- **Test helpers** (Phase 3): top-level functions declared after the last test in the same file, with a PHPDoc block and explicit return type (`tests/Feature/Authorization/OrganizationMembershipTest.php:183-194`); `tests/Pest.php` holds only the `something()` stub; names must be unique across all test files.
- **Blade style**: `@props([...])` on line 1, 4-space indentation, single-line anchors, `{{ $attributes->merge([...]) }}` for defaults, inline `@if ... @endif` inside tags for conditional attributes; prose uses typographic apostrophes (`’`) throughout, and the spec's new strings keep them.
- **Copy** (Phase 3): text begins immediately after the opening tag with no whitespace (`<p>You…`, `<h3>Build</h3><p>You…`); multi-paragraph sections often sit on one source line (`index.blade.php:46-48,53`, `walkthrough.blade.php:31`); first-person singular only — `rg -w -i 'we'` over the marketing views returns nothing; numbers are spelled out ("fifteen years"; the spec adds "three business days"); FAQ summaries end with `<x-marketing.arrow />`.
- **Records** (Phase 3): PRODUCT.md uses flat `-` bullets with typographic quotes under `##` headings; `.ai/rules/*.md` are YAML-frontmatter files (`paths:` list) written via Boost `record-rule` or edited directly with the frontmatter intact; `.ai/rules/index.md` maps globs to files.
- **Redirects**: `Route::permanentRedirect(from, to)` one-liners at the top of the marketing group (`routes/web.php:65-67`), tested in the dataset at `MarketingSiteTest.php:133-139`.
- **Local dev**: `php artisan dev` (not `serve`), probe `http://localhost:8000` (`.ai/rules/general.md:12`); marketing host resolves to `localhost` locally (`route:list` shows `localhost/...`); `.env` currently has `APP_ENV=local`, `APP_DEBUG=true`, `APP_URL=http://localhost:8000`, `POSTHOG_DISABLED=false` with a real token and host.
- **Git**: marketing work commits directly to `main` with linear history (`.ai/rules/general.md:9,15`); the working tree is clean apart from the untracked `docs/ideation/2026-09-17-walkthrough-offer-and-funnel-signals/` directory.

## Edge Cases

### Phase 3

- **`/walkthrough` ratio is an exact tie under the spec's copy** — a scratchpad simulation applied the spec's copy verbatim to the three templates and ran the spec's own regexes over page + layout: `/walkthrough` counts reader 39 vs first-person 39 (passes `>=` with zero margin), not the spec's "about 41 to 37"; `/` counts 21:19 with bare `strip_tags` (24:19 when tags become spaces). The spec's first fallback flip — step two "I ask questions until I understand the problem in its context." → "You get questions until the problem makes sense in its context." — moves `/walkthrough` to 40:37. Apply it (or the fit-answer flip) rather than shipping on a tie; the owner's first one-word edit at the gate would otherwise flip the test.
- **`strip_tags` merges words across adjacent tags** — `</h3><p>You` becomes `BuildYou`, so `\bYou\b` fails. Under the spec's homepage copy the three step openers at `index.blade.php:46-48` are invisible to the helper (24 → 21 reader); today the same effect hides three `I`s (18 → 15). It still passes, but if the metric should count what the reader reads, replace tags with a space before stripping (e.g. `preg_replace('/<[^>]+>/', ' ', $html)` in place of `strip_tags`) — a one-line gap decision; record the counts in the commit body either way.
- **Question test has zero margin by design** — homepage `<main>` contains 0 `?` today (index and invitation prose both 0); the spec's strip supplies exactly 2. The invitation's `?` heading is only passed on `/work` (`work.blade.php:30`). `preg_match('#<main[^>]*>(.*)</main>#s')` matches the single `<main>` at `layout.blade.php:36`; if it ever failed, `$main[1]` would be an undefined key — still a failure, just an uglier one.
- **Counts print only while failing** — the ratio's `reader words X vs first-person words Y on /path` is the `toBeGreaterThanOrEqual` failure message, so nothing prints once copy passes. Run the two new tests before any copy edit to see today's numbers (`/` 14:15 bare-strip or 14:18 spaced, `/walkthrough` 30:34 either way — the spec's "12 vs 16 / 30 vs 33" are estimates); to read a passing count, temporarily `dump()` or raise the threshold and remove it before committing.
- **Nav contributes 2 first-person words to every page** — `How I work` at `layout.blade.php:21,29`; layout alone is reader 0 / first-person 2. The spec's failure-mode row is correct: do not rename the nav to fix the ratio.
- **Helper placement and global uniqueness** — Pest loads all test files into one process; `marketingProse` must not collide (none today). Follow the precedent (after the last test, PHPDoc, `: string` return type) rather than the spec's top-of-file placement; both work.
- **Six anchors carry curly apostrophes** — `you’ll bring it up, not me`, `I’m not the right fit`, `the shape of the problem and where I’d look first`, `Where I’d start`, `room for work that’s worth their time`, and the existing `separate implementation engagement` neighbours. `assertSee` escapes a straight `'` to `&#039;` and fails against the template's `’`; copy from the template. The ratio helper normalises `’` → `'`; the anchors do not.
- **`Where I’d start` appears twice with different case** — `<dt>Where I’d start</dt>` and "…what I recommend, and where I’d start." in the hour FAQ. `assertSee` is case-sensitive, so the anchor matches only the `<dt>`; leave the FAQ lowercase.
- **New FAQ row must carry the arrow** — every `<summary>` at `walkthrough.blade.php:44-49` ends with `<x-marketing.arrow />` (DESIGN.md:228 documents the flip). Omitting it breaks the visual, not a test — the tests will not catch it.
- **Markup the tests pin** — `Book a free Walkthrough` (`MarketingSiteTest.php:21`), `id="choose-a-time"` and `href="#choose-a-time"` (`:39,42`), the Cal attribute strings at `:40-41`, `#how-i-work` (`:155,158`), and every `placement="…"` (`MarketingAnalyticsTest.php:27-31,36`). The spec's "keep the surrounding markup, classes, and ids" covers all of them.
- **`we` check is mechanical** — `rg -n -w -i 'we' resources/views/pages resources/views/components/marketing` returns nothing today and the spec's copy uses "you and I" instead; re-run after editing for the manual checklist item.
- **Scarcity grep is clean today and on the simulated copy** — exit 1 (no matches) both times; "give the people you already have room for work" does not hit the `while … room` alternative.
- **`no page still calls the offer a free assessment` stays green** — the new copy never says "free assessment"; the resources.md sentence "(the renamed free assessment)" is outside `resources/views`.
- **`/work` inherits the invitation rewrite** — `MarketingSiteTest.php:28-35` asserts `/work` does not say `retainer`, GHX, DataDash; the new invitation copy contains none.
- **PRODUCT.md has stale lines the spec does not list** — `:79` "free, roughly one-hour discovery assessment meeting", `:84-85` "Booking uses … `https://cal.com/birdcar/60min`", `:101-102` "the precise boundaries and turnaround for each report remain open". The spec appends at `:334-344` and prunes `:348-350` only, leaving `:84-85` contradicting the new "books through https://cal.com/birdcar/walkthrough" bullet in the same file. Gap decision: amend `:84-85` (and narrow `:101-102` to the paid report) in the same edit, or name the contradiction in the commit body.
- **`.ai/rules/resources.md:15` also says "free assessment/report offer"** — the spec replaces only the `:12` sentence; `:15` "Preserve approved copy, free assessment/report offer" would keep steering agents at the old name. Gap decision: change it to "free Walkthrough/report offer" in the same edit.
- **`record-rule` semantics are unverified** — Boost's tool takes glob/title/note; whether it replaces a sentence inside the existing "Approved marketing launch scope" section or appends a new block (possibly a new file, which also changes `index.md`) is unknown. If the result is not a single edited sentence at `:12`, use the spec's direct-edit fallback and keep the frontmatter `:1-4`.
- **Regex behaviour** — PHP's `u` modifier enables UCP, so `\b` is Unicode-aware; after `’`→`'` the apostrophe is non-word either way, so `you'll` counts once (via `you`) and `i'll` once (via `i`); `\bme\b` does not match inside `Menu`; FAQ visitor-voice words (`Do I need`, `my business`, `my team`) count as first-person exactly as the spec says.
- **Pint scope** — the only PHP change is the test file; `vendor/bin/pint --dirty --format agent` (preset `laravel`, `pint.json`).
- **Visual read needs the dev server, not Cal.com** — `php artisan dev` (`.ai/rules/general.md:12`); the `birdcar/walkthrough` slug is created at the Phase 4 gate, so the embed and fallback still fail locally by design.

### Phase 2

- **`route:list --name=public` cannot show `public.walkthrough`**: Folio pages are served by a single `Route::fallback` named `laravel-folio` (`FolioManager.php:74`), so `php artisan route:list --name=public` lists only the `web.php` routes (today: `/blog`, `/case-studies`, `/contact` redirects, `public.feed`, `public.sitemap` — 5 rows). The spec's Implementation step 4 and Manual Testing item "route:list shows public.walkthrough and no public.assessment" is half-unpassable as written; the equivalent check is `php artisan folio:list` showing `GET localhost/walkthrough .. public.walkthrough › walkthrough.blade.php` and no `/assessment` row, plus `route:list --name=public` showing 6 rows including `ANY localhost/assessment`.
- **In-process Folio name map on the long-running dev server**: `FolioRoutes` loads the name→file map once per instance (`FolioRoutes.php:61-66`). Under the FrankenPHP worker that `php artisan dev` runs, a worker that resolved `public.assessment` before the `git mv` may keep serving the stale map and throw `RouteNotFoundException` for `public.walkthrough` until restarted. Tests are unaffected (fresh process per run). If the browser 500s after the rename, restart `php artisan dev` before debugging the code.
- **Leftover page file cannot shadow the redirect**: because Folio is a fallback route, `Route::permanentRedirect('/assessment', '/walkthrough')` wins even if `assessment.blade.php` were accidentally left behind. A leftover would instead make both `public.assessment` and `public.walkthrough` resolvable and show two rows in `folio:list`; the spec's "no old file" mitigation still holds, `folio:list` is the detector.
- **No Folio route cache exists locally**: `bootstrap/cache/` contains only `packages.php` and `services.php`; `folio-routes.php` is written only by `route:cache` (`FolioServiceProvider.php:92-95`). Nothing to clear. On a deploy that runs `route:cache`, the cache regenerates from the new file set.
- **Redirect test: extend the dataset or add a new test**: `MarketingSiteTest.php:133-139` already asserts 301s via a dataset. Adding `['/assessment', '/walkthrough']` and retargeting `/contact` is the pattern-consistent move. The spec's check command `--filter='walkthrough'` matches the full Pest description including dataset arguments (`... with ('/assessment', '/walkthrough')`), so the dataset case should still run under that filter; the renamed `the walkthrough explains…` test guarantees the filtered suite is non-empty either way (`failOnEmptyTestSuite` is on).
- **`data-booking-cta="assessment-hero"` is neither renamed nor listed as kept**: the spec's name-strings table omits `assessment.blade.php:15`'s `placement="assessment-hero"`, and its "CSS class names stay" sentence does not mention the placement marker. `MarketingAnalyticsTest.php:36` asserts the current value. Neither validation grep matches it (`free assessment` / `free-assessment`). Phase 1 is not yet deployed, so no PostHog data depends on the value; renaming to `walkthrough-hero` now (with the test) is cheap, leaving it means the funnel's placement property carries the old word permanently. Gap decision for the builder; either way update or keep the test in the same commit.
- **`serviceType` stays**: `MarketingDiscoveryTest.php:49` asserts `serviceType => 'Business process assessment'`; the spec keeps it. Only `name` flips to `The Walkthrough`.
- **The negative test does not catch every old string**: `not->toContain('free assessment')` on lowercased HTML misses the page title `Book a free business assessment` (rendered three times via `<title>`, `og:title`, `twitter:title` from `MarketingSite::head():72-78`), the current meta description (contains no "assessment" at all), and `Your assessment report` / `Is this an AI assessment?` / `It’s an assessment of the work.` The spec's rename table covers all of them explicitly; do not rely on the negative test to find them.
- **Title change propagates through `Head`**: `title="Book a free Walkthrough"` becomes `<title>Book a free Walkthrough — Birdcar</title>` plus `og:title` and `twitter:title` (`MarketingSite.php:72-78`); no test asserts the old title string.
- **`$active === 'walkthrough'` must flip in three places at once**: `layout.blade.php:7,24,32`. Missing :24 or :32 turns the `/walkthrough` header/mobile CTA into the modal branch, which reintroduces `data-cal-config` on that page and fails `MarketingSiteTest.php:42` (`assertDontSee('data-cal-config')`); missing :7 drops the Service schema and fails `MarketingDiscoveryTest.php:49`.
- **Exact-string assertion on the inline div**: the new value at `MarketingSiteTest.php:39` must read `data-cal-inline data-cal-link="birdcar/walkthrough" data-cal-namespace="walkthrough"` verbatim, matching `assessment.blade.php:55` attribute order.
- **Sitemap count is unchanged**: `MarketingDiscoveryTest.php:102` expects 14 URLs; only the `/assessment` → `/walkthrough` loc string at :103 changes.
- **Host-404 dataset**: `MarketingDiscoveryTest.php:70` swaps to `['customer.birdcar.dev', '/walkthrough']`; since the new `/assessment` redirect lives inside the marketing `Route::domain` group, `customer.birdcar.dev/assessment` also still 404s.
- **Cal.com slug does not exist yet**: `birdcar/walkthrough` is created at the Phase 4 gate. Locally the embed will fail to load and the fallback link 404s on cal.com; Pest never loads Cal.com, so no test depends on it, and this phase's Feedback Strategy is the test suite only (no dev-server check required).
- **Pint scope**: the only PHP source edit is `MarketingSite.php` (parameter and string). `vendor/bin/pint --dirty --format agent` also formats the three test files.
- **Curly apostrophes in new strings**: the spec's strings (`It’s a Walkthrough of your work.`, `In a free Walkthrough, I’ll spend`) use `’`; `e()` does not escape it, so plain `assertSee` matches. Copy the character from the template rather than typing `'`.

### Phase 1

- **Blank token + debug throws before render**: `.env` has `APP_DEBUG=true` and `phpunit.xml` does not pin `APP_DEBUG`, so the blank-token test must set `config(['app.debug' => false])` before `$this->get()` (as the spec does) or `PostHogService::__construct` throws from the middleware at `PostHogRequestContext.php:13` and the request 500s before the layout runs.
- **Middleware resolves the service before the layout**: `PostHogRequestContext` is appended to the `web` group (`bootstrap/app.php:31`), so every marketing request constructs `PostHogService` once in middleware and again in the layout's `@php(app(...)->browserConfig())`. Both instances read the same test-time `config()` values; there is no cached singleton to invalidate.
- **Static `self::$initialized` persists across tests in one PHP process**: the first enabled test calls `PostHog::init('phc_test_token', ...)` and later tests never re-init with a different key. Harmless here (no capture runs), but a future server-side capture test with a different key would silently use the first key.
- **Pinning `POSTHOG_DISABLED=true` changes the test baseline**: today every feature test runs with PostHog enabled via `.env` (real token, `POSTHOG_DISABLED=false`), so `withContext` currently exercises the real client on every request; after the pin it short-circuits. Any test that implicitly relied on the enabled path would surface here — none were found.
- **Exact-string assertion on the inline div**: `MarketingSiteTest.php:39` asserts `data-cal-inline data-cal-link="birdcar/free-assessment" data-cal-namespace="free-assessment"` verbatim, so `data-booking-fallback` must go on the `<a>` at `assessment.blade.php:56`, not the `<div>` at :55.
- **Assessment page has three CTAs, all inline**: on `/assessment` the header and mobile-menu links render the inline branch (`href="#choose-a-time"`) and gain `header`/`mobile-menu` placements alongside the new `assessment-hero`; none open a modal, so `booking_embed_opened` there comes solely from the inline `linkReady` with `mode: inline` on page load. `booking.js:68-70` picks `source = inline ?? trigger`, so `mode` resolves to `inline` on that page and `modal` everywhere else.
- **Clicks that navigate may lose the capture**: a modal CTA clicked before `embed.js` registers `cal-modal-box` follows its `href` to `/assessment` (`booking.js:57`), and the `data-booking-fallback` anchor leaves for cal.com. posthog-js batches and flushes on `pagehide` via `sendBeacon` by default; confirm in the dev window that `booking_cta_clicked`/`booking_fallback_clicked` land in these two paths.
- **`forwardQueryParams` leaks control params into the Cal iframe**: `booking.js:79` sets `Cal.config.forwardQueryParams = true`, so `?ph_opt_out=1`, `?ph_opt_in=1`, and `?__posthog_debug=true` are forwarded to the Cal.com booker URL. Harmless, but visible in the iframe URL during the manual check.
- **Listener ordering and `preventDefault`**: `keepTriggersOnPage()` cancels navigation but not propagation; the analytics listener must not bail on `event.defaultPrevented`. Registering `initAnalytics()` before `initBooking()` in `app.js` guarantees it runs first anyway. The mobile-menu handler at `app.js:9-11` also listens on `nav a` clicks and is unaffected.
- **`event.target` inside the arrow SVG**: every CTA contains `<x-marketing.arrow />`; `Element.closest()` works from SVG descendants, matching the existing `event.target.closest('nav a')` usage at `app.js:10`.
- **Blade escaping of the host**: `{{ $posthog['host'] }}` HTML-escapes; `https://us.i.posthog.com` contains no characters that change under escaping, so `assertSee('data-posthog-host="https://us.i.posthog.com"', false)` matches.
- **`isEnabled()` has no consumer**: nothing in `app/`, `tests/`, the contract, or spec phases 2-3 calls it (rg over the three spec files and contract found only `browserConfig` at `contract.md:42`). Per the user's "delete unused code" preference the builder should either exercise it in a test or drop it and note the gap decision.
- **`?ph_opt_out=1` and canonical URLs**: `MarketingDiscoveryTest.php:14-21` proves query strings are stripped from canonical/og:url, so the opt-out visit does not perturb SEO metadata.
- **Octane**: the new accessor stores nothing per request; the existing static `$initialized` is process-level by design. Nothing new to guard.
- **Modifier clicks**: the delegated listener fires on ctrl/meta-clicks that open a new tab (browsers dispatch `click` for those), so a CTA opened in a background tab still records `booking_cta_clicked`; acceptable at this traffic level, but do not filter buttons unless the funnel shows noise.

## Risks

### Phase 3

- **The spec's expected ratio numbers are wrong and `/walkthrough` lands on a tie** — simulating the spec's copy verbatim gives 39:39 on `/walkthrough` (spec says ~41:37) and 21:19 on `/` (spec says ~20:18). Both pass `>=`, but a zero-margin metric will flip on the owner's first gate edit or any future FAQ addition. Take one of the spec's named fallback flips (step two → 40:37) and record the final counts in the commit body.
- **`marketingProse` undercounts at tag junctions** — bare `strip_tags` merges `</h3><p>You` into `BuildYou`; three reader words on the new homepage never register. Decide whether the metric should see them (`preg_replace('/<[^>]+>/', ' ', …)`) and note the choice; either way the test passes with the spec's copy.
- **Records edits leave contradictions if done literally** — `PRODUCT.md:84-85` still names `https://cal.com/birdcar/60min`, `:79` still calls the offer a "discovery assessment meeting", `:101-102` still says every report's turnaround is open, and `.ai/rules/resources.md:15` still says "free assessment/report offer". The spec touches only `PRODUCT.md:334-350` and `resources.md:12`; the next agent reading `:84-85` beside the new bullet gets two booking URLs. Amend or name the gap.
- **Decision log checked against reality — no contradictions** — "Where I'd start" not pre-empted (`walkthrough.blade.php:24` still reads `Something to work from`); no "business days", "discovery week", GitHub/Heroku/Zapier, or no-pitch line anywhere in `resources/views` (rg → nothing); Phase 2 rename landed in `a4c5099` with `walkthrough.blade.php`, `public.walkthrough`, `birdcar/walkthrough` config and tests, and `placement="walkthrough-hero"`; no `speaks to the reader`/`asks the reader` tests exist and `phpunit.xml:6` pins `failOnEmptyTestSuite="true"`, so the "Pest tests, not contract-only checks" decision is still open to implement; `PRODUCT.md:139,309-310` support naming three employers and omitting WorkOS; `PRODUCT.md:192-193` supports the replacing-people answer; the scarcity grep is clean; working tree clean apart from the untracked `docs/ideation/…` directory.
- **Test names are coupled to the contract** — `contract.md:26-27` filters on `speaks to the reader` and `asks the reader`; any paraphrase makes the acceptance run an empty suite, which now fails loudly rather than passing vacuously.
- **`record-rule` may be unavailable or may not edit in place** — the Boost MCP server is registered for this project but tool availability in the builder's session is unverified, and the tool's behaviour on an existing section is unknown. The spec's direct-edit fallback (keep `resources.md:1-4` frontmatter) is the safe path; verify `index.md:9` still maps `resources/**` to one file afterwards.
- **Tests do not judge the copy** — the anchors catch dropped commitments and the ratio catches regressions; the seven copy-editing sweeps and PRODUCT.md Written Voice (`:263-299`) are the owner's manual gate. Hype and "we" are only partly mechanised (scarcity grep, `rg -w we`).
- **Visual-only regression in the FAQ** — a new `<details>` without `<x-marketing.arrow />` renders wrong and no test notices.

### Phase 2

- **Spec manual check is wrong for Folio** — `php artisan route:list --name=public` will never list `public.walkthrough` (Folio pages ride a single fallback route, `FolioManager.php:74`); today it does not list `public.assessment` or `public.index` either. Treat the spec's step 4 / Manual Testing item as `php artisan folio:list` shows `/walkthrough` → `public.walkthrough` and no `/assessment`, and `route:list --name=public` gains one `ANY localhost/assessment` redirect row. Do not chase a phantom failure.
- **Stale project rule actively contradicts this phase** — `.ai/rules/resources.md:12` says "Assessment booking uses https://cal.com/birdcar/60min and the free meeting/report offer" and `:15` says "Preserve approved copy, free assessment/report offer". Its glob (`resources/**`) covers every Blade file in this phase. The contract's decision log (`contract.md:86-87`) supersedes it and Phase 3 (`spec-phase-3.md:238-240`) rewrites the rule; the builder should follow the contract and not "correct" the rename back, and should not update the rule in this phase (Phase 3 owns it).
- **Decision log checked against reality — no contradictions** — `/assessment` is still a live Folio page and `config/marketing.php:6-8` still reads `free-assessment`, so the "Full tier moves the URL in this phase" and "move config to birdcar/walkthrough" decisions have not been pre-empted; Phase 1 is committed (`d75312a`, `MarketingAnalyticsTest.php` present, `browserConfig()` at `layout.blade.php:15`, `data-booking-cta` markers on every CTA), so the "sequential after Phase 1" premise holds; the working tree is clean.
- **Placement marker `assessment-hero` is an unlogged gap** — the spec neither renames nor explicitly keeps `placement="assessment-hero"` (`assessment.blade.php:15`, asserted at `MarketingAnalyticsTest.php:36`). Decide and note it in the commit body; no validation grep will flag either choice.
- **Three `$active` comparisons in one file** — `layout.blade.php:7,24,32` must all flip to `'walkthrough'` with `assessment.blade.php:8`'s `active="walkthrough"`; a partial edit fails `MarketingSiteTest.php:42` or `MarketingDiscoveryTest.php:49` and is easy to misread as a different bug.
- **Dev server may serve a stale Folio name map** — `FolioRoutes` holds the name map per instance (`FolioRoutes.php:61-66`); under the FrankenPHP worker a `RouteNotFoundException` for `public.walkthrough` after the `git mv` means restart `php artisan dev`, not a code error. Tests are unaffected.
- **Cal.com `birdcar/walkthrough` does not exist until the Phase 4 gate** — the local embed and fallback link break by design after this phase; no test depends on Cal.com, and this phase must not deploy alone (`spec-phase-2.md:164`).
- **Phase 3 depends on this phase's exact names** — `spec-phase-3.md:8,46,76,168` reference `resources/views/pages/walkthrough.blade.php`, `/walkthrough`, and the button string `Book a free Walkthrough`. Deviating from any of them (for example keeping "my" in the hero button) breaks Phase 3's spec.

### Phase 1

- **Local dev already emits to production PostHog** — `.env` has `POSTHOG_DISABLED=false` and a real token while `.ai/rules/general.md:12` recommends `POSTHOG_DISABLED=true` for day-to-day work. Every `php artisan dev` session during this phase creates real persons/events; use a private window and record its person for deletion at the gate as the spec's Open Items require.
- **Cal.com event names and payload are unverified** — `linkReady` per-open behaviour and the `bookingSuccessfulV2` payload shape are documented only in the spec's own notes (no offline reference in the repo). The spec's fallback (`bookerViewed`) and named-destructuring mitigation stand; record the observed behaviour in the commit body.
- **`MarketingSiteTest.php:36-56` is the regression net for the Blade edits** — the exact-string assertion at :39 and the `assertDontSee('data-cal-config')` at :42 fail if `data-booking-fallback` lands on the wrong element or the hero anchor is converted to the modal branch instead of `inline`.
- **No JS test runner or linter** — `package.json` has only `build`/`dev`; the browser half is verified solely by `bun run build` + bundle grep and the manual debug-window steps. Regressions in `analytics.js`/`booking.js` will not be caught automatically.
- **`bun add posthog-js` modifies the tracked `bun.lock`** (76 KB text lockfile) and adds a new runtime dependency — the Boost guidelines say dependency changes need approval; the contract's decision log already records this choice, so cite it in the commit.
- **Stale project rule, out of scope** — `.ai/rules/resources.md:12` still says booking uses `https://cal.com/birdcar/60min`, while `config/marketing.php:6-8` and `MarketingSiteTest.php:39-43` use `birdcar/free-assessment`. Not a Phase 1 concern (Phase 2 renames the event), but do not let the rule steer the fallback URL.
- **Decision log checked against reality — no contradictions**: `FolioServiceProvider.php:16-17` binds Folio to the marketing host and `MarketingDiscoveryTest.php:66-73` proves marketing pages 404 on `admin.`/`customer.` (supports dropping the host clause); `assessment.blade.php:15` and `booking-link.blade.php:3` carry no `data-cal-link` (supports the `data-booking-cta` marker decision); `phpunit.xsd:233` declares `failOnEmptyTestSuite` (supports the pin); no CDN snippet or `posthog-js` exists anywhere (consistent with the bundled-dependency decision); the contract confirms the Full tier (`contract.md:52,98`), matching the spec's `booking_fallback_clicked` and `mode` additions.
